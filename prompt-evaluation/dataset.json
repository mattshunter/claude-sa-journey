[ {
  "task" : "Create a regex pattern that validates AWS S3 bucket names according to AWS naming rules: 3-63 characters long, lowercase letters, numbers, hyphens, and dots only, must start and end with a letter or number, and cannot have consecutive dots",
  "format" : "regex"
}, {
  "task" : "Write a Python function that takes a list of EC2 instance dictionaries and returns only the instances that are in 'running' state, sorted by launch time",
  "format" : "python"
}, {
  "task" : "Create a JSON object representing an AWS Lambda function configuration with a 256MB memory allocation, 30-second timeout, Python 3.9 runtime, and environment variables for DATABASE_URL and API_KEY",
  "format" : "json"
} ]